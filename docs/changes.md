# A List of Changes

1. PDNSolution.hpp

  1. GetLocalArray( std::vector<double> ) function is changed to std::vector<double> GetLocalArray().

2. ALocal_IEN.hpp
  1. get_LIEN_e functions are renamed as get_LIEN.
  2. get_LIEN( const int &elem, std::vector<int> &elem_ien ) is changed to std::vector<int> geT_LIEN( const int &elem ).
